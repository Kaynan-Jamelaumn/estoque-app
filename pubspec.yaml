name: flutter_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  sqflite: ^2.3.3
  sqflite_common_ffi: ^2.3.3
  sqflite_common_ffi_web: ^1.1.1
  path: ^1.9.0
  path_provider: ^2.1.3
  provider: ^6.1.2

  intl: ^0.19.0
  uuid: ^4.4.0


  # Fotos de produto
  image_picker: ^1.1.2

  # Código de barras
  mobile_scanner: ^5.2.3
  barcode_widget: ^2.0.4

  # Gráficos do dashboard
  fl_chart: ^0.68.0

  # Compartilhar/gerar relatórios
  #share_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
